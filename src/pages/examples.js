import React from 'react';
import { Layout } from '../Components/Layout';

export default () => (
    <Layout title="Examples" disableHeader>
        <main className="container">
            <section className="section">
                <h1>Examples React Spring!</h1>
            </section>
        </main>
    </Layout>
)